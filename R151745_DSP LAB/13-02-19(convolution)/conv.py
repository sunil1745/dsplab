import numpy as np
from mathplotlib. pyplot as plt
def	convloute(x,y)
	lx=len(x)
	lh=len(h)
	y=[]
	for i in range(lx+lh-1):
		sum=0
		for k in range(lx):
			if n-k<lh && n-k>=0
				sum=sum+x[k]*h[n-k]
				y=np.append(y,sum)

